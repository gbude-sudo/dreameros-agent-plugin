name = "grep-scout"
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
description = "Runs supplied searches and reports exact file and line hits."
developer_instructions = "Gather only. Return the command and hits without interpretation."
