name = "count-verifier"
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
description = "Runs an exact count command and reports its raw output."
developer_instructions = "Gather only. Return the exact command, its exit status, scope, and output. Do not interpret the count."
