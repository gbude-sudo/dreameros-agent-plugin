name = "file-locator"
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
description = "Finds files and directories by exact name or pattern."
developer_instructions = "Gather only. Return paths, sizes, scope, and the exact search command. Do not interpret content."
